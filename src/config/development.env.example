NODE_ENV=development

HOST=localhost
PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_NAME=referi_dev
DB_USER_NAME=root
DB_USER_PASSWORD=toor

JWT_SECRET=somethingSecret
MAIL_API_KEY=

# When set, all emails are redirected to this address instead of the real recipient
DEBUG_EMAIL=

# Set to true to skip all email sending — codes and passwords are printed to the API console instead
SKIP_EMAIL=true

API_DOC_USER=someuser
API_DOC_PASS=somepassword
